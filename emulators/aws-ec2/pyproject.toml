[project]
name = "vera-aws"
version = "1"
description = "A local AWS EC2 service emulator for development and testing"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0",
]

[project.scripts]
vera-aws = "main:main"
