# One Server — GCP Compute instance with tags

Deploys a single Compute Engine VM with a resource tag.

## Usage

```bash
export GOOGLE_BACKEND_OVERRIDE=http://localhost:9100
terraform init && terraform apply -auto-approve
```
